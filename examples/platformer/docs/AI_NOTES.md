# Platformer — AI Notes

- Start with `docs.get_relevant` for any feature request.
- Player movement lives in `scripts/player.controller.ts`.
- Coin pickup lives in `scripts/coin.collectable.ts`.
- Prefer updating scene JSON / props over rewriting engine code.
- After script edits, run `build.get_errors` (includes typecheck).
