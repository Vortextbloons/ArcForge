# Collectable — AI usage

1. Ensure the plugin is enabled (`plugin.list` / `plugin.enable`).
2. Add `game.collectable` via `scene.update_component` or prefab create.
3. Keep pickup logic in a small Behaviour script — do not fork the engine.
