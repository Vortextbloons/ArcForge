# ThreeForge Vite Game Template

This folder is a scaffold used by `@threeforge/exporter` when creating an editable Three.js project.

Do not run it directly — export a game project into a new output directory instead.
